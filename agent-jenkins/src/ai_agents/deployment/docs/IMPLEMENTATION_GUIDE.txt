================================================================================
                    GUIDE D'IMPLÉMENTATION - API AI AGENTS
================================================================================

Ce document explique en détail l'implémentation de l'API REST pour le framework
AI Agents, les fonctions créées, et les relations entre les fichiers.

================================================================================
1. ARCHITECTURE GÉNÉRALE
================================================================================

L'API suit une architecture en couches :

┌─────────────────────────────────────────────────────────────────┐
│                        COUCHE PRÉSENTATION                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   main.py       │  │  start_api.py   │  │   routes/       │ │
│  │ (FastAPI App)   │  │  (Démarrage)    │  │ (Endpoints)     │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
┌─────────────────────────────────────────────────────────────────┐
│                        COUCHE MÉTIER                           │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   services/     │  │    models/      │  │     core/       │ │
│  │ (Logique)       │  │ (Validation)    │  │ (Configuration) │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
┌─────────────────────────────────────────────────────────────────┐
│                        COUCHE DONNÉES                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Agent Registry  │  │  Memory Store   │  │   File System   │ │
│  │ (Agents)        │  │ (Exécutions)    │  │ (Configs)       │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

================================================================================
2. STRUCTURE DES FICHIERS ET LEURS RÔLES
================================================================================

📁 api_agent/
├── 📄 main.py                    # Point d'entrée FastAPI principal
├── 🚀 start_api.py               # Script de démarrage du serveur
├── ⚡ quick_start.py             # Démarrage rapide avec vérifications
├── 🔧 test_config.py             # Test de configuration
├── 🎬 demo.py                    # Démonstration des fonctionnalités
├── 🎊 final_demo.py              # Démonstration finale complète
├── 📚 README.md                  # Documentation utilisateur
├── 📝 IMPLEMENTATION_GUIDE.txt   # Ce fichier (guide technique)
│
├── 📁 core/                      # Configuration et utilitaires
│   ├── __init__.py
│   ├── config.py                 # Settings Pydantic (OPENAI_API_KEY, etc.)
│   └── logging.py                # Configuration des logs
│
├── 📁 api/                       # Couche API (routes et modèles)
│   ├── __init__.py
│   ├── 📁 models/                # Modèles Pydantic pour validation
│   │   ├── __init__.py
│   │   └── agent.py              # Modèles d'agents et exécutions
│   └── 📁 routes/                # Endpoints FastAPI
│       ├── __init__.py
│       └── agents.py             # Routes pour gestion des agents
│
├── 📁 services/                  # Logique métier
│   ├── __init__.py
│   └── agent_service.py          # Service principal de gestion d'agents
│
└── 📁 examples/                  # Exemples et clients
    └── api_examples.py           # Client Python et exemples d'usage

================================================================================
3. DÉTAIL DES FICHIERS PRINCIPAUX
================================================================================

3.1. main.py - Application FastAPI
----------------------------------
RÔLE : Point d'entrée principal de l'application FastAPI

FONCTIONS CLÉS :
- create_app() : Crée et configure l'application FastAPI
- lifespan() : Gestionnaire du cycle de vie de l'application
- Configuration CORS pour permettre les requêtes cross-origin
- Inclusion des routes d'agents
- Endpoints de base (/, /health)

RELATIONS :
- Importe les routes depuis api/routes/agents.py
- Utilise la configuration depuis core/config.py
- Configure le logging via core/logging.py

3.2. core/config.py - Configuration
-----------------------------------
RÔLE : Gestion centralisée de la configuration avec Pydantic

CLASSES :
- Settings : Modèle Pydantic pour toutes les configurations

CHAMPS PRINCIPAUX :
- HOST, PORT : Configuration serveur
- DEBUG, LOG_LEVEL : Configuration debug et logs
- OPENAI_API_KEY : Clé API OpenAI (OBLIGATOIRE)
- ALLOWED_HOSTS : Configuration CORS
- MAX_EXECUTION_TIME : Timeout des exécutions
- AGENTS_CONFIG_DIR : Répertoire des configurations d'agents

FONCTION :
- get_settings() : Singleton pour obtenir la configuration

PROBLÈME RÉSOLU :
Le champ OPENAI_API_KEY a été ajouté pour résoudre l'erreur Pydantic
"Extra inputs are not permitted" que vous rencontriez.

3.3. services/agent_service.py - Logique métier
-----------------------------------------------
RÔLE : Service principal gérant toute la logique des agents

CLASSE PRINCIPALE : AgentService

ATTRIBUTS :
- running_agents : Dict des agents en cours d'exécution
- agent_runs : Dict de toutes les exécutions (persistance mémoire)
- settings : Configuration de l'application

MÉTHODES PRINCIPALES :

list_agents() -> List[AgentInfo]
  • Liste tous les agents disponibles depuis AGENT_REGISTRY
  • Crée des objets AgentInfo avec métadonnées

get_agent_details(agent_id) -> AgentDetailedInfo
  • Obtient les détails complets d'un agent
  • Charge la configuration depuis le fichier YAML
  • Retourne config + métadonnées

get_agent_config(agent_id) -> AgentConfig
  • Retourne uniquement la configuration d'un agent
  • Extrait workflow, state_schema, memory, model

execute_agent(agent_id, run_id, input_data, config_override, timeout)
  • FONCTION CŒUR : Exécute un agent de manière asynchrone
  • Gère le cycle de vie complet d'une exécution
  • Applique les timeouts et surcharges de configuration
  • Persiste les résultats dans agent_runs

get_agent_status(agent_id, run_id) -> AgentStatusResponse
  • Retourne le statut actuel d'un agent
  • Gère les agents en cours ET terminés
  • Trouve automatiquement la dernière exécution si run_id non spécifié

list_agent_runs(agent_id) -> List[AgentRunResponse]
  • Liste toutes les exécutions d'un agent
  • Triées par date (plus récent en premier)

get_run_details(agent_id, run_id) -> AgentRunResponse
  • Détails d'une exécution spécifique
  • Inclut logs de debug pour troubleshooting

cancel_run(agent_id, run_id) -> bool
  • Annule une exécution en cours
  • Met à jour le statut et nettoie les ressources

SINGLETON PATTERN :
Le service utilise un pattern singleton dans les routes pour persister
les données d'exécution entre les requêtes HTTP.

3.4. api/models/agent.py - Modèles de données
---------------------------------------------
RÔLE : Définition des modèles Pydantic pour validation et sérialisation

ENUMS :
- AgentStatus : idle, running, completed, failed, cancelled
- AgentType : sales_assistant, supervisor, research

MODÈLES PRINCIPAUX :

AgentInfo
  • Informations de base d'un agent
  • Champs : id, name, type, description, version, status

AgentConfig
  • Configuration complète d'un agent
  • Champs : workflow, state_schema, memory, model

AgentDetailedInfo (hérite d'AgentInfo)
  • Informations détaillées + configuration
  • Ajoute : config, created_at, updated_at

AgentRunRequest
  • Requête pour lancer un agent
  • Champs : input_data, config_override, timeout

AgentRunResponse
  • Réponse d'exécution d'agent
  • Champs : run_id, agent_id, status, started_at, completed_at,
           result, error, logs

AgentStatusResponse
  • Statut d'un agent
  • Champs : agent_id, run_id, status, last_activity, current_run

AgentListResponse
  • Liste d'agents
  • Champs : agents, total

ErrorResponse
  • Modèle d'erreur standardisé
  • Champs : error, detail, code

3.5. api/routes/agents.py - Endpoints REST
------------------------------------------
RÔLE : Définition des endpoints HTTP pour l'API

SINGLETON SERVICE :
- get_agent_service() : Retourne une instance singleton d'AgentService
  pour persister les données entre requêtes

ENDPOINTS IMPLÉMENTÉS :

GET /api/agents
  • Liste tous les agents disponibles
  • Retourne : AgentListResponse

GET /api/agents/{agent_id}
  • Détails d'un agent spécifique
  • Retourne : AgentDetailedInfo
  • Erreur 404 si agent inexistant

GET /api/agents/{agent_id}/config
  • Configuration d'un agent
  • Retourne : AgentConfig
  • Erreur 404 si agent inexistant

POST /api/agents/{agent_id}/run
  • Lance l'exécution d'un agent
  • Body : AgentRunRequest
  • Retourne : AgentRunResponse (statut initial)
  • Exécution en arrière-plan via BackgroundTasks

GET /api/agents/{agent_id}/status
  • Statut d'un agent
  • Query param optionnel : run_id
  • Retourne : AgentStatusResponse

GET /api/agents/{agent_id}/runs
  • Liste toutes les exécutions d'un agent
  • Retourne : List[AgentRunResponse]

GET /api/agents/{agent_id}/runs/{run_id}
  • Détails d'une exécution spécifique
  • Retourne : AgentRunResponse
  • Erreur 404 si run inexistant

DELETE /api/agents/{agent_id}/runs/{run_id}
  • Annule une exécution en cours
  • Retourne : Message de confirmation

GESTION D'ERREURS :
- Validation automatique via Pydantic
- Gestion des exceptions avec messages clairs
- Codes HTTP appropriés (404, 422, 500)
- Logging des erreurs pour debug

================================================================================
4. FLUX D'EXÉCUTION D'UN AGENT
================================================================================

1. CLIENT fait POST /api/agents/{agent_id}/run
   ↓
2. ROUTE agents.py valide la requête (Pydantic)
   ↓
3. ROUTE vérifie que l'agent existe via agent_service.agent_exists()
   ↓
4. ROUTE génère un run_id unique (UUID)
   ↓
5. ROUTE lance agent_service.execute_agent() en arrière-plan
   ↓
6. ROUTE retourne immédiatement AgentRunResponse (status: running)
   ↓
7. SERVICE charge l'agent depuis AGENT_REGISTRY
   ↓
8. SERVICE applique config_override si fournie
   ↓
9. SERVICE exécute agent.run(input_data) avec timeout
   ↓
10. SERVICE met à jour agent_runs avec le résultat
    ↓
11. CLIENT peut interroger GET /api/agents/{agent_id}/status pour suivre

================================================================================
5. GESTION DE LA PERSISTANCE
================================================================================

PROBLÈME INITIAL :
Les données d'exécution n'étaient pas persistées entre les requêtes car
un nouveau AgentService était créé à chaque appel.

SOLUTION IMPLÉMENTÉE :
- Pattern Singleton dans get_agent_service()
- Instance globale _agent_service conservée en mémoire
- Dictionnaires agent_runs et running_agents persistent les données

STRUCTURE DES DONNÉES :
agent_runs = {
    "sales_assistant": {
        "run-id-1": AgentRunResponse(...),
        "run-id-2": AgentRunResponse(...),
    },
    "research": {
        "run-id-3": AgentRunResponse(...),
    }
}

running_agents = {
    "sales_assistant": {
        "run_id": "run-id-1",
        "started_at": datetime,
        "status": AgentStatus.RUNNING
    }
}

LIMITATIONS :
- Persistance en mémoire uniquement (perdue au redémarrage)
- Pour la production : utiliser Redis, PostgreSQL, ou autre DB

================================================================================
6. SCRIPTS UTILITAIRES
================================================================================

6.1. start_api.py
-----------------
- Démarre le serveur Uvicorn
- Affiche les informations de configuration
- Gestion d'erreurs avec messages d'aide

6.2. test_config.py
-------------------
- Valide la configuration avant démarrage
- Teste les imports et dépendances
- Vérifie OPENAI_API_KEY

6.3. quick_start.py
-------------------
- Script interactif de démarrage
- Vérifications automatiques
- Lance le serveur ET la démonstration

6.4. demo.py / final_demo.py
----------------------------
- Démonstrations complètes des fonctionnalités
- Tests de tous les endpoints
- Exemples d'utilisation réelle

6.5. examples/api_examples.py
-----------------------------
- Client Python pour l'API
- Classe AgentAPIClient avec toutes les méthodes
- Exemples d'utilisation basique et avancée
- Gestion d'erreurs

================================================================================
7. POINTS TECHNIQUES IMPORTANTS
================================================================================

7.1. Gestion Asynchrone
-----------------------
- FastAPI avec async/await
- BackgroundTasks pour exécutions longues
- asyncio.wait_for() pour timeouts

7.2. Validation des Données
---------------------------
- Pydantic pour validation automatique
- Modèles typés pour toutes les entrées/sorties
- Gestion d'erreurs 422 automatique

7.3. Configuration
------------------
- Pydantic Settings avec .env
- Variables d'environnement
- Validation des types

7.4. Logging
------------
- Logging structuré
- Niveaux configurables
- Logs d'exécution pour debug

7.5. CORS
---------
- Configuration pour développement
- Permettre requêtes cross-origin

================================================================================
8. EXTENSIONS POSSIBLES
================================================================================

- Authentification JWT
- Base de données pour persistance
- WebSockets pour notifications temps réel
- Métriques et monitoring
- Rate limiting
- Cache Redis
- Tests unitaires et d'intégration
- Documentation OpenAPI enrichie

================================================================================
FIN DU GUIDE D'IMPLÉMENTATION
================================================================================
